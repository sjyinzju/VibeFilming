# P5R — Reference gate calibration and Hero recovery

## Current scope (user correction, 2026-09-10)

General VibeFilming architecture takes priority. This film is an integration test;
75 seconds, eight shots and all scenes/dialogue are no longer acceptance requirements.
The fixture now uses a configurable three-shot/20-second candidate policy. Failed shots
remain HUMAN_REVIEW / OMIT_FROM_CANDIDATE while independent shots continue.
The user accepted the exact v3 portrait plus v5 wardrobe references with known limitations.
The generic ReferenceResolver composes their semantic roles; original VLM negatives stay intact.
See [architecture checkpoint](p5r_architecture_checkpoint.md) for implementation and remaining evidence.

## Historical plan and observations (superseded where stated above)

Same project: `project_58ad9203c92a4e4fb8c3fe18b8d1a839`. No model download, baseline replay,
old acceptance mutation, TTS/music regeneration, human approval or P6.

1. Role-specific reference usability, explicit policy/config revision, concrete blocking evidence.
2. Preserve18 prior inspection reservations. New authorized policy has separate provider-dispatch
   and committed-material-failure ledgers: invalid evidence/transport consumes only provider budget.
   At most3 outer critic dispatches/config/subject and6 across configurations, at most2 committed material failures; each provider
   dispatch retains its existing maximum2 semantic attempts. Unknown completion is not replayed.
   Committed inconclusive reviews without a concrete major/critical defect are not proven material
   failures. The user subsequently explicitly authorized technical-review supersession: “允许技术审核被新版本替代”.
   Original review records remain preserved; obsolete empty-timeline and proven invalid-schema reviews
   may be superseded for valid reinspection. Final aesthetic approval remains pending.
   Config2 fixes the verified optional-evidence schema defect; material budgets never reset.
3. Ella v1 reused. FLUX regenerates Lynne/Fall/Style under the same artifact IDs, new versions.
   Sky and Suspender reuse images for role-correct reinspection. No repeated structural Kontext removal.
4. Per-reference and per-shot recovery nodes are appended to the existing DAG. Visible characters,
   location and visible/relevant props are hard; style is soft. Frame→gate→H3→critics progress independently.
5. Eight story-core shots total75s and include the three existing dialogue cues. Candidate eligibility
   requires8 accepted shots,75s, coverage of all3 scenes and all canonical spoken cues. Export preempts
   optional work; actual film critic still checks coherence. Optional shots backfill failures.
6. Existing real video/critic/repair/post contracts run unchanged where possible. Structural frame
   failures route to FLUX; small correctable lighting/style to finite Kontext; minor defects remain warnings.
7. Run targeted policy/budget/resolver/routing/progress tests before real dispatch. After a real candidate,
   run full regression and verify exact MP4/SRT/stems/manifest downloads, then update Morning Review.

No policy revision may be invented merely to repeat the same failed material attempt. The persisted
authorization records this user's P5R scope and exact policy revision. Old results are never rewritten.

The user explicitly authorized “允许最后一次修复和审核” after seeing that Lynne v4 still contains a hood.
Only `identity_CHAR_LYNNE_001` now permits3 material failures. Its FLUX cap remains3 new generations,
critic cap remains3/config and6 total, and all other reference/H3/audio budgets remain unchanged.
The final full-body FLUX prompt specifies a flat round neckline and exposed rear neck using positive geometry.

Verified recovery observations (2026-09-10): config2 produced valid results for Lynne v3, Sky v2 and Suspender v1. Suspender passes unchanged. Lynne v3 lacks the visible full costume; Sky v2 lacks a visible air gap. One FLUX successor for each is scheduled through new DAG repair nodes, preserving their negative results and pending old-version reviews. Pending shot dependencies are rebound to the new material gate, which must pass independently. No old material is accepted.
